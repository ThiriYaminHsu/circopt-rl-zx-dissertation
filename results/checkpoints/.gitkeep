# Placeholder so this directory is tracked by git.
#
# Model checkpoints (.pt files) generated by scripts/train_agent_r.py and
# scripts/train_agent_a.py land here and are gitignored -- see .gitignore.
# For large checkpoints, prefer attaching them to a GitHub Release instead of
# committing them directly.
